# Copy this file to .env and fill in real values. Never commit the real .env file.

# MongoDB connection string (local or MongoDB Atlas)
MONGODB_URI=mongodb+srv://vu241fa04710_db_user:<Varshith@2025>@cluster0.tcgle3a.mongodb.net/?appName=Cluster0

# Secret used to sign login tokens — use a long random string in production
JWT_SECRET=change-this-to-a-long-random-string

# Fixed admin login (as requested)
ADMIN_USERNAME=Varshith@2025
ADMIN_PASSWORD=9381803389

# Port the server listens on (hosting providers usually set this automatically)
PORT=4000
